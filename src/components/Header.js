import React from 'react';

function Header(props) {
    return (
        <header className="head">
            <h1 className="head-title">TEST APP</h1>
        </header>
    )
};

export default Header